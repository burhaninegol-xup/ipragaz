{
  "name": "ipragaz-bayi",
  "version": "1.0.0",
  "description": "Ipragaz Bayi Yonetim Sistemi",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^24.0.0"
  }
}
